{
  "stack": "cedar-14",
  "environments": {
    "test": {
      "formation": {
        "test": {
          "quantity": 1,
          "size": "standard-1x"
        }
      },
      "buildpacks": [
        {
          "url": "https://github.com/heroku/heroku-buildpack-xvfb-google-chrome"
        },
        { "url": "heroku/nodejs" }
      ],
      "scripts": {
        "test-setup": "apt-get install libgtk2.0-0 libgtk-3-0 libnotify-dev libgconf-2-4 libnss3 libxss1 libasound2 libxtst6 xauth xvfb",
        "test": "npm test"
      }
    }
  }
}
